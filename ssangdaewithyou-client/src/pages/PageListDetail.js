import React from 'react';
import ListDetail from '../components/ListDetail';

function PageListDetail({ existingPlaceInfo }) {
  
  return (
    <ListDetail existingPlaceInfo={existingPlaceInfo}/>
  )
}

export default PageListDetail;